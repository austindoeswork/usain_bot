#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../google-image-search/bin/google-image-search" "$@"
  ret=$?
else 
  node  "$basedir/../google-image-search/bin/google-image-search" "$@"
  ret=$?
fi
exit $ret
